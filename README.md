# set-tf-alias

Portable bash/zsh shell library that automatically switches `tf`, `tofu`, and
`terraform` aliases based on `.opentofu-version` / `.terraform-version` files
and `tenv` integration.

Full documentation coming soon — see
[the design spec](https://github.com/langburd/set-tf-alias) while in development.
